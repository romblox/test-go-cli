### Usage of the CLI tool:

```shell
go run main.go -url https://manjaro.org -threads 10 -timeout 5
```

Where the first argument is the `url`, second argument is the `number of requests` and the third argument is `timeout` seconds.  
